# docker-compose.yml

version: '3'
services:
  database:
    image: "postgres:alpine"
    ports:
      - "5432:5432"
    env_file:
      - database.env
    volumes:
      - database-data:/var/lib/postgresql/reportas/data/

volumes:
  database-data:
